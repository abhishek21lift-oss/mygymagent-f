"use client"

import * as React from "react"
import { CalendarDays, CheckCircle2, Clock3, Dumbbell, Sparkles, UserRound, Users, XCircle, Zap } from "lucide-react"
import { toast } from "sonner"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { useBranches } from "@/lib/hooks/use-branches"
import { useMembers } from "@/lib/hooks/use-members"
import { useStaff } from "@/lib/hooks/use-staff"
import { ApiError } from "@/lib/api/client"
import { useBookPtSession, usePtSessionAction, usePtSessions, type PtSession, type PtSessionType } from "@/lib/hooks/use-pt-sessions"

function statusVariant(status:string){if(status==="COMPLETED")return "success" as const;if(status==="CANCELLED"||status==="NO_SHOW")return "destructive" as const;return "default" as const}
function SessionRow({session}:{session:PtSession}){const complete=usePtSessionAction("complete"),cancel=usePtSessionAction("cancel"),noShow=usePtSessionAction("no-show");const busy=complete.isPending||cancel.isPending||noShow.isPending;const member=session.member?`${session.member.firstName} ${session.member.lastName}`:"Member unavailable";const trainer=session.trainer?`${session.trainer.firstName} ${session.trainer.lastName}`:"Unassigned";async function run(action:"complete"|"cancel"|"no-show"){try{if(action==="complete")await complete.mutateAsync({id:session.id});if(action==="cancel")await cancel.mutateAsync({id:session.id,reason:"Cancelled by trainer"});if(action==="no-show")await noShow.mutateAsync({id:session.id});toast.success(action==="complete"?"PT session completed":action==="no-show"?"Marked as no-show":"Session cancelled")}catch(e){toast.error(e instanceof ApiError?e.message:"Unable to update session")}}return <div className="group flex flex-col gap-4 rounded-[22px] border border-stone-200/80 bg-white/75 p-4 transition hover:-translate-y-0.5 hover:border-violet-200 hover:bg-white hover:shadow-lg lg:flex-row lg:items-center"><div className="flex min-w-0 flex-1 items-center gap-3"><span className="flex size-11 shrink-0 items-center justify-center rounded-[15px] bg-gradient-to-br from-cyan-100 to-violet-100 text-violet-700"><UserRound className="size-5"/></span><div className="min-w-0"><p className="truncate text-sm font-extrabold text-stone-900">{member}</p><p className="truncate text-xs font-medium text-stone-500">{session.member?.memberCode??""} · {trainer}</p></div></div><div className="flex items-center gap-2 text-xs font-semibold text-stone-600"><Clock3 className="size-4 text-violet-500"/><span>{new Date(session.startTime).toLocaleString(undefined,{day:"numeric",month:"short",hour:"numeric",minute:"2-digit"})}</span><span className="text-stone-300">→</span><span>{new Date(session.endTime).toLocaleTimeString([], {hour:"numeric",minute:"2-digit"})}</span></div><div className="flex items-center gap-2"><Badge variant={statusVariant(session.status)}>{session.status.replace("_"," ")}</Badge><Badge variant="outline">{session.type.replaceAll("_"," ")}</Badge></div>{session.status==="SCHEDULED"&&<div className="flex gap-2 lg:ml-auto"><Button size="sm" disabled={busy} onClick={()=>run("complete")}><CheckCircle2 className="size-4"/>Complete</Button><Button size="sm" variant="outline" disabled={busy} onClick={()=>run("no-show")}><XCircle className="size-4"/>No-show</Button><Button size="sm" variant="ghost" disabled={busy} onClick={()=>run("cancel")}>Cancel</Button></div>}</div>}

export default function PtSessionsPage(){const today=new Date(),dateKey=today.toISOString().slice(0,10),startFrom=new Date(`${dateKey}T00:00:00`).toISOString(),endTo=new Date(`${dateKey}T23:59:59`).toISOString();const sessions=usePtSessions({page:1,pageSize:100,order:"asc",startFrom,endTo}),members=useMembers({page:1,pageSize:100,order:"asc"}),staff=useStaff({page:1,pageSize:100,order:"asc"}),branches=useBranches({page:1,pageSize:100,order:"asc"}),book=useBookPtSession();const [memberId,setMemberId]=React.useState(""),[trainerId,setTrainerId]=React.useState(""),[branchId,setBranchId]=React.useState(""),[start,setStart]=React.useState(`${dateKey}T10:00`),[end,setEnd]=React.useState(`${dateKey}T11:00`),[type,setType]=React.useState<PtSessionType>("PERSONAL_TRAINING"),[price,setPrice]=React.useState("");const items=sessions.data?.items??[],scheduled=items.filter(x=>x.status==="SCHEDULED"),completed=items.filter(x=>x.status==="COMPLETED"),noShows=items.filter(x=>x.status==="NO_SHOW");async function submit(e:React.FormEvent){e.preventDefault();if(!memberId||!branchId)return toast.error("Select a client and branch");if(new Date(start)>=new Date(end))return toast.error("End time must be after start time");try{await book.mutateAsync({memberId,trainerId:trainerId||undefined,branchId,startTime:new Date(start).toISOString(),endTime:new Date(end).toISOString(),type,price:price?Number(price):undefined});toast.success("PT session booked");setPrice("")}catch(err){toast.error(err instanceof ApiError?err.message:"Unable to book PT session")}}
return <div className="relative -mx-2 min-h-full overflow-hidden pb-12 sm:-mx-3 lg:-mx-5"><div className="pointer-events-none absolute inset-0 -z-10 bg-[radial-gradient(circle_at_5%_2%,rgba(6,182,212,.13),transparent_19%),radial-gradient(circle_at_96%_4%,rgba(99,102,241,.15),transparent_22%),radial-gradient(circle_at_70%_45%,rgba(217,70,239,.10),transparent_25%),radial-gradient(circle_at_12%_80%,rgba(16,185,129,.08),transparent_24%)]"/><div className="mx-auto flex max-w-[1680px] flex-col gap-8 px-2 sm:px-4 lg:px-6"><section className="relative overflow-hidden rounded-[34px] border border-white/90 bg-white/88 p-6 shadow-[0_35px_110px_-48px_rgba(79,70,229,.48)] backdrop-blur-2xl sm:p-8 lg:p-10"><div className="pointer-events-none absolute -left-24 -top-32 size-80 rounded-full bg-cyan-300/25 blur-3xl"/><div className="pointer-events-none absolute -right-24 -top-24 size-96 rounded-full bg-violet-300/25 blur-3xl"/><div className="relative flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-cyan-100 bg-white/75 px-3 py-1.5 text-[10px] font-black uppercase tracking-[.18em] text-cyan-700"><Sparkles className="size-3.5"/> Live coaching calendar</div><h1 className="font-serif text-4xl font-semibold tracking-[-.045em] text-stone-950 sm:text-5xl">PT Sessions</h1><p className="mt-3 max-w-2xl text-sm font-medium leading-6 text-stone-500">Book, execute and close today&apos;s personal-training sessions without leaving the training command center.</p></div><Button variant="outline" asChild className="rounded-2xl border-stone-200 bg-white/80"><a href="/pt-operations"><Dumbbell className="size-4"/> PT OS</a></Button></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={CalendarDays} label="Scheduled" value={scheduled.length} hint="Sessions still to deliver"/><Metric icon={CheckCircle2} label="Completed" value={completed.length} hint="Closed today"/><Metric icon={XCircle} label="No-shows" value={noShows.length} hint="Needs follow-up"/><Metric icon={Clock3} label="Total" value={items.length} hint="Today&apos;s session volume"/></section><section className="grid gap-5 xl:grid-cols-[.82fr_1.45fr]"><Card className="overflow-hidden border-white/90 bg-white/88 shadow-xl shadow-violet-900/5 backdrop-blur-xl"><CardHeader className="border-b border-stone-100/80 bg-gradient-to-r from-white via-cyan-50/40 to-violet-50/50"><CardTitle className="font-serif text-xl">Book a PT session</CardTitle><p className="text-xs text-stone-500">Create a real calendar session.</p></CardHeader><CardContent className="pt-5"><form className="flex flex-col gap-4" onSubmit={submit}><Field label="Client"><select value={memberId} onChange={e=>setMemberId(e.target.value)} className="input"><option value="">Select client</option>{(members.data?.items??[]).map(m=><option key={m.id} value={m.id}>{m.firstName} {m.lastName} · {m.memberCode}</option>)}</select></Field><Field label="Trainer"><select value={trainerId} onChange={e=>setTrainerId(e.target.value)} className="input"><option value="">Unassigned</option>{(staff.data?.items??[]).filter(s=>s.staffProfile?.isTrainer).map(s=>s.staffProfile&&<option key={s.staffProfile.id} value={s.staffProfile.id}>{s.firstName} {s.lastName}</option>)}</select></Field><Field label="Branch"><select value={branchId} onChange={e=>setBranchId(e.target.value)} className="input"><option value="">Select branch</option>{(branches.data?.items??[]).map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></Field><div className="grid grid-cols-2 gap-3"><Field label="Start"><input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)} className="input"/></Field><Field label="End"><input type="datetime-local" value={end} onChange={e=>setEnd(e.target.value)} className="input"/></Field></div><div className="grid grid-cols-2 gap-3"><Field label="Session type"><select value={type} onChange={e=>setType(e.target.value as PtSessionType)} className="input"><option value="PERSONAL_TRAINING">Personal</option><option value="PARTNER_TRAINING">Partner</option><option value="SMALL_GROUP">Small group</option></select></Field><Field label="Price"><input type="number" min="0" value={price} onChange={e=>setPrice(e.target.value)} placeholder="₹" className="input"/></Field></div><Button type="submit" className="mt-1 w-full rounded-2xl bg-[linear-gradient(105deg,#4338ca,#7c3aed_52%,#c026d3)]" disabled={book.isPending}>{book.isPending?"Booking...":"Book PT session"}</Button></form></CardContent></Card><Card className="overflow-hidden border-white/90 bg-white/88 shadow-xl shadow-cyan-900/5 backdrop-blur-xl"><CardHeader className="border-b border-stone-100/80 bg-gradient-to-r from-white via-emerald-50/30 to-cyan-50/40"><CardTitle className="font-serif text-xl">Today&apos;s coaching timeline</CardTitle><p className="text-xs text-stone-500">Actual PT sessions, ordered by start time.</p></CardHeader><CardContent className="p-3 sm:p-4">{sessions.isError?<div className="rounded-2xl bg-rose-50 p-5 text-sm font-semibold text-rose-700">Unable to load PT sessions.</div>:items.length===0&&!sessions.isLoading?<div className="rounded-2xl border border-dashed border-stone-200 bg-stone-50/70 p-10 text-center"><CalendarDays className="mx-auto size-8 text-stone-400"/><p className="mt-3 font-bold text-stone-900">No PT sessions today</p><p className="mt-1 text-xs text-stone-500">Book the first coaching session from the panel.</p></div>:<div className="flex flex-col gap-3">{items.map(x=><SessionRow key={x.id} session={x}/>)}</div>}</CardContent></Card></section></div></div>}
function Metric({icon:Icon,label,value,hint}:{icon:typeof Users;label:string;value:React.ReactNode;hint:string}){return <Card className="relative overflow-hidden border-white/90 bg-white/85 shadow-[0_20px_60px_-38px_rgba(79,70,229,.35)] backdrop-blur-xl"><div className="pointer-events-none absolute -right-8 -top-8 size-28 rounded-full bg-violet-200/30 blur-2xl"/><CardContent className="relative flex items-center gap-4 p-5"><span className="flex size-14 items-center justify-center rounded-[19px] bg-gradient-to-br from-cyan-100 to-violet-100 text-violet-700"><Icon className="size-6"/></span><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-stone-400">{label}</p><p className="mt-1 text-2xl font-black text-stone-950">{value}</p><p className="text-[11px] text-stone-400">{hint}</p></div></CardContent></Card>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="text-sm font-bold text-stone-700">{label}<div className="mt-1.5">{children}</div></label>}
